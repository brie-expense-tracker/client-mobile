import React from 'react';
import { SafeAreaView } from 'react-native';
import PasswordResetTest from '../../../../src/components/PasswordResetTest';

export default function TestPasswordResetScreen() {
	return (
		<SafeAreaView style={{ flex: 1 }}>
			<PasswordResetTest />
		</SafeAreaView>
	);
}
